{% for name,repo in distro.rhel[ansible_distribution_version].repos.iteritems() %}
[{{repo.section}}]
name={{repo.name}}
baseurl={{repo.baseurl}}
gpgcheck={{ repo.gpgcheck }}
failovermethod=priority
enabled=1

{% endfor %}
